const {modelo, wiFi, ram, procesador, redMovil, fecha} = require('./celular.js')

console.log(`Mi celular es un ${modelo}, si me preguntas si tiene Wi-fi ese es un '${wiFi} fact', su ram es de ${ram},
su procesador es el ${procesador}, tiene internet movil ${redMovil} y mira me indica la fecha dice que es ${fecha}`);